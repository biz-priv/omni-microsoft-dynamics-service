seslambda:
  timeout: 300
  handler: dynamics-crm-prod.lambda_handler
  environment:
    DevURL: ${ssm:/Dynamics-URL-dev}
    ProdURL: ${ssm:/Dynamics-URL-prod}
    DevSNS: ${ssm:/Dynamics-SNS-dev}
    ProdSNS: ${ssm:/Dynamics-SNS-prod}